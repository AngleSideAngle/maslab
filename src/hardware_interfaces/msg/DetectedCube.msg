# x, y, width, and height are measured in pixels and relative to the camera
# frame (Z+ is forward, X+ is right and Y+ is up?) (wtf)
# distance is a scalar in meters

float32 distance
int32 x
int32 y
int32 width
int32 height
